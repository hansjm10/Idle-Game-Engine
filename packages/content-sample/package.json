{
  "name": "@idle-engine/content-sample",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint 'src/**/*.ts'",
    "test": "vitest",
    "test:ci": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@idle-engine/content-compiler": "workspace:*",
    "@idle-engine/content-schema": "workspace:*",
    "@idle-engine/core": "workspace:*"
  },
  "devDependencies": {
    "@idle-engine/config-eslint": "workspace:*",
    "@idle-engine/config-vitest": "workspace:*",
    "@types/node": "^24.9.1",
    "eslint": "^9.37.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "vitest-llm-reporter": "^1.2.0"
  }
}
