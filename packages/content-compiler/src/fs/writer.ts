import type { PackArtifactResult, WorkspaceFS } from '../types.js';

export async function writePackArtifacts(
  fs: WorkspaceFS,
  result: PackArtifactResult,
): Promise<void> {
  void fs;
  void result;
}
