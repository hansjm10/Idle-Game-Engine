import { z } from 'zod';

/**
 * Placeholder resources schema awaiting detailed modelling.
 */
export const resourceDefinitionSchema = z.unknown();
export const resourceCollectionSchema = z.unknown();
