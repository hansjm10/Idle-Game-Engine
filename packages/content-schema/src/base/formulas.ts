import { z } from 'zod';

/**
 * Placeholder formula support. Future work will introduce full AST validation.
 */
export const formulaNodeSchema = z.unknown();
export const numericFormulaSchema = z.unknown();
