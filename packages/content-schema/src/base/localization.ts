import { z } from 'zod';

/**
 * Placeholder localisation schemas. Real implementations will follow §5.2 guidance.
 */
export const localizedStringSchema = z.unknown();
export const localizationMapSchema = z.unknown();
