{
  "name": "@idle-engine/content-validation-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "tsx ./src/generate.ts",
    "compile": "tsx ./src/compile.ts",
    "test": "vitest",
    "test:ci": "vitest run --passWithNoTests",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@idle-engine/content-compiler": "workspace:*",
    "@idle-engine/content-schema": "workspace:*",
    "@idle-engine/core": "workspace:*",
    "chokidar": "^3.6.0",
    "json5": "^2.2.3"
  },
  "devDependencies": {
    "@idle-engine/config-vitest": "workspace:*",
    "@types/node": "^24.9.1",
    "fast-check": "^4.3.0",
    "tsx": "^4.20.6",
    "vitest": "^3.2.4",
    "vitest-llm-reporter": "^1.2.0"
  }
}
