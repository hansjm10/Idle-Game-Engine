{
  "name": "@idle-engine/content-schema-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node ./src/generate.js",
    "compile": "node ./src/compile.js",
    "test": "vitest",
    "test:ci": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@idle-engine/content-compiler": "workspace:*",
    "@idle-engine/content-schema": "workspace:*",
    "chokidar": "^3.6.0",
    "json5": "^2.2.3"
  },
  "devDependencies": {
    "@idle-engine/config-vitest": "workspace:*",
    "vitest": "^3.2.4",
    "vitest-llm-reporter": "^1.2.0"
  }
}
